function [D, Ind] = dijkstra(lengths)
% Алгоритм Дейкстры

n = size(lengths, 1);
D = inf(n);
Ind = zeros(n, 1);

% j = 1
D(1) = 0;

% j = 2+
for j = 2:1:n
    for i = 1:1:n
        disp(4)
        
    end






end

